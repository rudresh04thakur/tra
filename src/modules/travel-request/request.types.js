/**
 * RequestController
 * @typedef {import('./request.controller')} RequestController
 */

/**
 * RequestService
 * @typedef {import('./request.service')} RequestService
 */

/**
 * RequestValidator
 * @typedef {import('./request.validator')} RequestValidator
 */
